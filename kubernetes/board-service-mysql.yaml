apiVersion: v1
kind: PersistentVolumeClaim
metadata:
  name: board-service-mysql-pvc
spec:
  accessModes:
    - ReadWriteOnce
  resources:
    requests:
      storage: 1Gi
---
apiVersion: apps/v1
kind: Deployment
metadata:
  name: board-service-mysql
spec:
  selector:
    matchLabels:
      app: board-service-mysql
  strategy:
    type: Recreate
  template:
    metadata:
      labels:
        app: board-service-mysql
    spec:
      containers:
      - image: mysql:latest
        name: board-service-mysql
        env:
        - name: MYSQL_ROOT_PASSWORD
          value: "1234"
        - name: MYSQL_DATABASE
          value: "board_service"
        ports:
        - containerPort: 3306
          name: mysql
        args:
        - "--character-set-server=utf8mb4"
        - "--collation-server=utf8mb4_unicode_ci"
        volumeMounts:
        - name: board-service-mysql-persistent-storage
          mountPath: /var/lib/mysql
      volumes:
      - name: board-service-mysql-persistent-storage
        persistentVolumeClaim:
          claimName: board-service-mysql-pvc
---
apiVersion: v1
kind: Service
metadata:
  name: board-service-mysql
spec:
  ports:
  - port: 3306
    targetPort: 3306
  selector:
    app: board-service-mysql
  type: ClusterIP