server:
  port: 8000

spring:
  application:
    name: lucky-gateway
  cloud:
    gateway:
      routes:
        - id: user-service
          uri: ${USER_SERVICE_URL:http://localhost:8081}
          predicates:
            - Path=/user-service/**
          filters:
            - StripPrefix=1